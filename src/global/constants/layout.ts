/** Tinggi bottom navigation dari theme override (MuiBottomNavigation root height). */
export const APP_BOTTOM_NAV_HEIGHT = 80;

/** Perkiraan tinggi sticky mission CTA bar di halaman practice. */
export const MISSION_STICKY_CTA_HEIGHT = 68;
